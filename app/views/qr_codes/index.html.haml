%section.content
  Adding a QR code is quick and easy.
  .link
    = link_to "Create One Now!", new_qr_code_path, :class => 'button'

%section.recent
  %header Random QR Code Of The Moment
  = render :partial => 'qr_codes/qr_code', :locals => {:qr_code => @qr_code, :small => true }
